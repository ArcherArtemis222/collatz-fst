本專案的作業規約集中在 [AGENTS.md](./AGENTS.md)。動手前請完整讀過該檔。

重點速記：
- `Core/` 凍結，只有專案主人能改。
- 不得 `lake update`，不得動 `lean-toolchain`。
- 不得 `sorry` / `native_decide` / `axiom`。
- 一律 branch → PR，不直接 push `main`。
- `lake build` 綠了才 commit。
